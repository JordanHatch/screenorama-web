<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) + " - " : "" %>Screenorama</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div id="wrap">
    <header>
      <h1><%= link_to 'Screens', root_path %></h1>

      <nav class="screens">
        <%= render partial: 'layouts/screen_list', locals: { screens: screens } %>

        <% if new_screens.any? %>
          <h2>New screens</h2>
          <%= render partial: 'layouts/screen_list', locals: { screens: new_screens, edit_links: true } %>
        <% end %>
      </nav>

      <p>Connect a screen to the internet and it will appear in this list.</p>
    </header>

    <main>
      <%= yield %>
    </main>
  </div>

</body>
</html>
